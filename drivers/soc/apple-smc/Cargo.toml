[package]
name = "scarlet-driver-apple-smc"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
fdt = "0.1.5"
scarlet = { path = "../../../kernel" }
scarlet-driver-apple-asc = { path = "../apple-asc" }
scarlet-driver-apple-rtkit = { path = "../apple-rtkit" }
